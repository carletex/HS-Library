extends layout.jade

block title
  title HS Library
block content
  h1 Hello from Express + Jade template!
  div(ng-controller="bookController")
    div(ng-view)
      ul
        li(ng-repeat="book in books") {{ book }}